const note_url = `http://localhost:3080`
const blog_url = `http://localhost:3080`
const mock_url = `/mock`
const sso_url = 'http://localhost:3380'

export enum BASE_URL {
  NOTE = note_url,
  BLOG = blog_url,
  MOCK = mock_url,
  SSO = sso_url
}
